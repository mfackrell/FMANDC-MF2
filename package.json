{
  "name": "@fmandc/website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R index.html assets site-images favicon.ico tracking.js robots.txt sitemap.xml llms.txt _redirects services portfolio about testimonials contact intake dist/",
    "dev": "vite",
    "preview": "vite preview",
    "serve": "npx serve ."
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
